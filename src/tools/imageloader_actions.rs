use web_sys::HtmlElement;

pub fn is_visible(image: Option<HtmlElement>, rs_src: String) {
    // Check if the image is in view of the user
    // Return a boolean or an error -> Result<bool, JsValue>
    todo!()
}
